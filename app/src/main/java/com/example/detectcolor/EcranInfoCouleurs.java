package com.example.detectcolor;

import android.graphics.Typeface;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.text.SpannableString;
import android.text.Spanned;
import android.text.style.StyleSpan;
import android.widget.TextView;

public class EcranInfoCouleurs extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_ecran_info_couleurs);

       //TextView textView = findViewById(R.id.text);

       // String text = "Les couleurs opposées correspondent à la couleur complémentaire et deux autres proches, opposées à couleur sélectionnée sur le cercle chromatique. ";

      //  SpannableString ss = new SpannableString(text);

      //  StyleSpan boldSpan =  new StyleSpan(Typeface.BOLD);

      //  ss.setSpan(boldSpan, 0, 19, Spanned.SPAN_EXCLUSIVE_EXCLUSIVE);

      //  TextView.setText(ss);

    }
}